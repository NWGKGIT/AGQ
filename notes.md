- SLOP DESIGN FIXES. 
- highlight on the email cards. the side-tab card thick colored boder on one side is slop and indicator of AI generated UI. 
- add more hover effects. 
- linear/vercel look
- Potential logic errors, like next reset in now
- font sizing mismatch in 'next reset "minutes" "email"' 
it says next poll in 0m in the dashboard
once you click an account it should show you a better looking details sidebar instead of what it is now. what's wrong now is once you click the email card the thing you see is gemini pool, openai pool, antrhopic pool and then it's just blank, like '-' only. I don't see any percentages. 
and there's also a 'usage history - 7 days'   which isn't what I want, I want to see the percentage of each model I have left, colored and clear. also the detail sidebar could use some more coloring and hover effects + add polled date.

for design look at 'https://impeccable.style/docs/' 

also the logic, like the snapshots etc, is it working? like what happens if I logged out of an account and no snapshot was taken for a week, does it refill after the reset is hit? is it smart in general.
 Write an extensive documentation in docs/ about everything, techicals and logic flow etc. 
